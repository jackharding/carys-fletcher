import React from 'react';

interface IWorkItemProps {
  title: string;
  cover: string;
}

const WorkItem = ({ title, cover }: IWorkItemProps): React.ReactNode => {
  return null;
}

export default WorkItem;